inherit: true

tools:
    external_code_coverage: true
    sensiolabs_security_checker:
        enabled: true

filter:
    excluded_paths:
        - 'vendor/*'
        - 'tests/*'
        - 'app/*'
        - 'bin/*'
        - 'library/*'
        - 'Tests/*'
